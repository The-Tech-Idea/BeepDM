# MASTER-TODO-TRACKER — Beep Logging & Audit-Trail Services

> Single source of truth for both `IBeepLog` and `IBeepAudit` implementation.
> Cross-link every TODO to its phase doc. Mark `[x]` only after the verification criteria in the phase doc are met.

Legend: `[ ]` open · `[~]` in-progress · `[x]` done · `[!]` blocked

---

## Phase 00 — Overview & Gap Matrix · [`00-overview-log-audit-gap-matrix.md`](./00-overview-log-audit-gap-matrix.md)

- [ ] P00-01 Inventory all existing logging touchpoints (`IDMLogger`, `DMLogger`, `Microsoft.Extensions.Logging`).
- [ ] P00-02 Inventory all existing audit touchpoints (`AuditManager`, `IProxyAuditSink`, planned distributed sinks).
- [ ] P00-03 Produce gap matrix and v1 scope freeze.
- [ ] P00-04 Define non-goals and v2 deferrals.

## Phase 01 — Core Contracts & Feature Toggles · [`01-phase1-core-contracts-and-feature-toggles.md`](./01-phase1-core-contracts-and-feature-toggles.md)

- [x] P01-01 Create `Services/Logging/` and `Services/Audit/` and `Services/Telemetry/` folders.
- [x] P01-02 `IBeepLog.cs` (cross-platform contract). _(Models · `DataManagementModelsStandard/Services/Logging/IBeepLog.cs` + `BeepLogLevel.cs`)_
- [x] P01-03 `IBeepAudit.cs` (cross-platform contract). _(Models · `DataManagementModelsStandard/Services/Audit/IBeepAudit.cs` + `Models/AuditEvent.cs` + `Models/AuditQuery.cs`)_
- [x] P01-04 `BeepLoggingOptions.cs` + `BeepAuditOptions.cs`. _(Engine · `Services/Logging/`, `Services/Audit/`)_
- [x] P01-05 `BeepServiceExtensions.Logging.cs` (`AddBeepLogging`). _(Engine · `Services/BeepServiceExtensions.Logging.cs`)_
- [x] P01-06 `BeepServiceExtensions.Audit.cs` (`AddBeepAudit`). _(Engine · `Services/BeepServiceExtensions.Audit.cs`)_
- [x] P01-07 Wire opt-in flags into `BeepService` lifecycle (`Start`/`Stop`). _(satisfied by Phase 02: production pipeline registered as keyed `IAsyncDisposable` singleton; DI container drains and disposes on host shutdown)_
- [x] P01-08 No-op default implementations (`NullBeepLog`, `NullBeepAudit`) for when feature is disabled. _(Engine · `Services/Logging/NullBeepLog.cs`, `Services/Audit/NullBeepAudit.cs`)_

> Phase 01 build status: net9.0 build succeeds with **0 errors**. `BackpressureMode` enum + `TelemetryFeature` constants also landed under `Services/Telemetry/` to seed Phase 02.

## Phase 02 — Shared Pipeline, Sinks, Async Batching · [`02-phase2-shared-pipeline-sinks-and-batching.md`](./02-phase2-shared-pipeline-sinks-and-batching.md)

- [x] P02-01 `Services/Telemetry/TelemetryEnvelope.cs` (universal record). _(Engine · `Services/Telemetry/TelemetryEnvelope.cs` + `TelemetryKind.cs`)_
- [x] P02-02 `Services/Telemetry/ITelemetrySink.cs`. _(Engine · `ITelemetrySink.cs` + forward-decl `IEnricher.cs`, `IRedactor.cs`, `ISampler.cs`)_
- [x] P02-03 `Services/Telemetry/BoundedChannelQueue.cs` (bounded `Channel<T>`). _(Engine · `BoundedChannelQueue.cs`; honors `DropOldest` / `Block` / `FailFast` via `BoundedChannelOptions`)_
- [x] P02-04 `Services/Telemetry/BatchWriter.cs` (async drain, batching, flush interval). _(Engine · `BatchWriter.cs`; per-sink try/catch isolation, drain-on-dispose)_
- [x] P02-05 `Services/Telemetry/TelemetryPipeline.cs` (compose enrichers/redactors/samplers/sinks). _(Engine · `TelemetryPipeline.{Core,Drain,Flush}.cs` partials)_
- [x] P02-06 Backpressure policy: `DropOldest` | `Block` | `FailFast`. _(implemented in `BoundedChannelQueue.TryEnqueue` / `EnqueueAsync`; logs use sync TryEnqueue, audit uses async EnqueueAsync)_
- [x] P02-07 Hook pipeline into `IBeepLog` and `IBeepAudit` (audit forces `Block` by default). _(Engine · `BeepLog.{Core,Levels,Lifetime}.cs`, `BeepAudit.{Core,Query,Lifetime}.cs`; extensions register a per-feature pipeline as keyed singleton)_

> Phase 02 build status: net9.0 build succeeds with **0 errors / 0 warnings** in incremental build. Logging and audit each get their own `TelemetryPipeline` instance (different backpressure modes); both pipelines are registered as keyed singletons (`Beep.Logging.Pipeline`, `Beep.Audit.Pipeline`) so the DI container handles disposal at host shutdown. Audit query/purge/integrity surface throws `NotImplementedException` pointing to Phases 08 & 10. `BeepAudit` exposes both `Task RecordAsync(...)` (interface contract) and a `ValueTask RecordValueTaskAsync(...)` fast-path for the hot audit producer.

## Phase 03 — Storage Providers (Cross-Platform) · [`03-phase3-storage-providers-cross-platform.md`](./03-phase3-storage-providers-cross-platform.md)

- [x] P03-01 `Services/Telemetry/Sinks/MemorySink.cs`. _(Engine · ring buffer, soft cap, `Snapshot()` for tests)_
- [x] P03-02 `Services/Telemetry/Sinks/NullSink.cs`. _(Engine · singleton no-op)_
- [x] P03-03 `Services/Telemetry/Sinks/FileRollingSink.cs` (NDJSON, size+time roll). _(Engine · `FileRollingSink.{Core,Write,Roll}.cs` partials + `RolledFile.cs` event payload + `NdjsonSerializer.cs`)_
- [x] P03-04 `Services/Telemetry/Sinks/SqliteSink.cs` (single-file, WAL). _(Engine · `SqliteSink.{Core,Schema,Write,Query}.cs` partials; `Microsoft.Data.Sqlite 10.0.6` added; WAL + synchronous=NORMAL/FULL toggle; transaction-per-batch; partial indexes for audit-only columns)_
- [x] P03-05 `Services/Telemetry/Sinks/CompositeSink.cs` (fan-out). _(Engine · per-inner try/catch + `IsHealthy` aggregate)_
- [x] P03-06 Cross-platform path resolver `Services/Telemetry/PlatformPaths.cs`. _(Engine · `LocalApplicationData` + `SpecialFolderOption.Create`; `LogsDir` / `AuditDir` / `SanitizeFileName`)_
- [~] P03-07 Smoke tests for each sink. _(deferred to Phase 13 alongside P02-08; same rationale: no test project yet under `BeepDM/`)_

> Phase 03 build status: net9.0 build succeeds with **0 errors**. No new warnings come from any new file under `Services/Telemetry/`. NuGet adds `Microsoft.Data.Sqlite 10.0.6` (≈1.5 MB transitive cost called out in the phase doc R1) — only loaded when the `SqliteSink` type is referenced. `FileRollingSink` raises a `Rolled` event after every rotation so Phase 04 (compression + retention + budget sweeper) can subscribe without modifying the sink. `SqliteSink` schema v1 stores logs and audit in a single denormalized `telemetry` table with partial indexes on audit-only columns; schema version is persisted in the `meta` table for the Phase 08 chain-seal migration.

## Phase 04 — Retention, Rotation, Compression, Storage Budget · [`04-phase4-retention-rotation-compression-budgeting.md`](./04-phase4-retention-rotation-compression-budgeting.md)

- [x] P04-01 `Services/Telemetry/Retention/RotationPolicy.cs` (size/time/event-count). _(Engine · POCO + `Clone()`)_
- [x] P04-02 `Services/Telemetry/Retention/RetentionPolicy.cs` (days + max files). _(Engine · POCO + `Clone()`)_
- [x] P04-03 `Services/Telemetry/Retention/StorageBudget.cs` (total directory cap). _(Engine · POCO + `BudgetBreachAction.cs` enum)_
- [x] P04-04 Gzip compression on rotate (`GzipStream`, cross-platform). _(Engine · `DefaultBudgetEnforcer.Compress.cs` subscribes to `FileRollingSink.Rolled`; deletes the raw file on success; idempotent re-runs)_
- [x] P04-05 Sweeper job (timer-driven, opt-in interval). _(Engine · `RetentionSweeperHostedService` over `BackgroundService` + `PeriodicTimer`; runs an immediate pass on startup; `AddBeepRetentionSweeper(TimeSpan?)` opt-in)_
- [x] P04-06 `Services/Telemetry/Retention/IBudgetEnforcer.cs` + default impl (delete-oldest first). _(Engine · `IBudgetEnforcer.cs` + `DefaultBudgetEnforcer.{Core,Sweep,Compress}.cs` partials; `EnforcerScope.cs`, `BudgetSweepResult.cs` payloads; per-scope `IsBlocked` flag for `BlockNewWrites` mode; exclusive-lock probe before delete to avoid clobbering an in-use file)_
- [~] P04-07 Tests: budget breach, retention age, max files, gzip compress. _(deferred to Phase 13 alongside P02-08 / P03-07; same rationale: no test project under `BeepDM/` yet)_

> Phase 04 build status: net9.0 build succeeds with **0 errors / 0 new warnings**. `BeepLoggingOptions` and `BeepAuditOptions` now expose `Rotation`, `Retention`, `Budget`, `EnableRetentionSweeper`, and `SweepInterval`. The logging registration helper auto-attaches every `FileRollingSink` it finds in `options.Sinks` to a `DefaultBudgetEnforcer` scope (logs default to `DeleteOldest`; audit defaults to `BlockNewWrites` to protect compliance). `FileRollingSink` was extended with `Directory`, `Prefix`, `Extension`, and `FilePattern` getters so the scope can be derived without re-passing constructor arguments. `IBudgetEnforcer` is registered as a singleton via either `AddBeepBudgetEnforcer()` (manual sweep) or `AddBeepRetentionSweeper()` (timer + manual).

## Phase 05 — Redaction, PII & Secret Scrubbing · [`05-phase5-redaction-pii-and-secret-scrubbing.md`](./05-phase5-redaction-pii-and-secret-scrubbing.md)

- [x] P05-01 `Services/Telemetry/Redaction/IRedactor.cs`. _(forward-decl `Services/Telemetry/IRedactor.cs` from Phase 02; Phase 05 adds `RedactionMode.cs`, `RedactionContext.cs`, internal `RedactionHelpers.cs` for shared `Mask`/`Hash`/`Transform` primitives, and a partial `TelemetryEnvelope.Clone()` to support per-sink decoration)_
- [x] P05-02 `RegexRedactor`, `KeywordRedactor`, `KeyValueRedactor`. _(Engine · `Services/Telemetry/Redaction/RegexRedactor.cs`, `KeywordRedactor.cs`, `KeyValueRedactor.cs`; word-boundary anchored, case-insensitive, compiled-once regex)_
- [x] P05-03 `StructuredFieldRedactor` (operates on `IDictionary<string,object>` properties). _(Engine · `Services/Telemetry/Redaction/StructuredFieldRedactor.cs`; `RedactionMode.Drop` removes the key; `Mask`/`Hash` coerce via `ToString`)_
- [x] P05-04 Bridge `Proxy/ProxyLogRedactor.cs` into shared redactor pipeline. _(Engine · `Services/Telemetry/Redaction/ProxyRedactorAdapter.cs`; reuses the legacy compiled regex set; emits an additional `ExceptionMessageRedacted` property when an exception is attached)_
- [x] P05-05 Built-in patterns: connection strings, tokens, JWTs, credit cards, emails (opt-in). _(Engine · `ConnectionStringRedactor.cs`, `JwtRedactor.cs`, `CreditCardRedactor.cs` with Luhn pre-filter, `EmailRedactor.cs`; `CompositeRedactor.cs` bundles chains; `DefaultRedactionPresets.cs` ships `Off()`, `LogsBalanced()` (Mask), `AuditStrict()` (Hash for PII keys, Mask for tokens))_
- [x] P05-06 Allow per-sink redactor stacks (audit may need stricter rules than logs). _(Engine · `Services/Telemetry/Sinks/RedactingSinkDecorator.cs`; clones each envelope before applying its sink-local chain so fan-out siblings see the global-only redaction)_
- [~] P05-07 Tests for every built-in + presets + per-sink stack. _(deferred to Phase 13 alongside P02-08 / P03-07 / P04-07; same rationale: no test project under `BeepDM/` yet)_

> Phase 05 build status: net9.0 build succeeds with **0 errors / 0 new warnings**. The pipeline integration from Phase 02 already runs the global `IRedactor` chain (`TelemetryPipeline.ApplyRedactors`); per-sink stacks are opt-in by wrapping the inner sink in a `RedactingSinkDecorator(inner, redactors)` and adding the wrapper to `options.Sinks` in place of the bare sink. R3 (operator forgetting to enable redaction) is deferred to Phase 11 so the once-on-startup warning can flow through `PipelineMetrics`. `TelemetryEnvelope` becomes `partial` — the `Clone()` helper lives in its own `TelemetryEnvelope.Clone.cs` so the property surface stays in `TelemetryEnvelope.cs`.

## Phase 06 — Enrichment, Correlation, Ambient Context · [`06-phase6-enrichment-correlation-context.md`](./06-phase6-enrichment-correlation-context.md)

- [x] P06-01 `Services/Telemetry/Context/BeepActivityScope.cs` (`AsyncLocal<Stack<Activity>>`). _(Engine · `BeepActivityScope.{Core,AsyncLocal}.cs` partials + `BeepActivity.cs` POCO + `IdGenerators.cs` for W3C-compatible 32/16 hex ids; out-of-order Dispose tolerated; `NoopScope` for empty names)_
- [x] P06-02 `IEnricher` + built-in: correlationId, traceId, spanId, machine, process, thread, env. _(forward-decl `Services/Telemetry/IEnricher.cs` from Phase 02 stays canonical to keep the pipeline contract stable; Phase 06 adds `CorrelationEnricher.cs`, `TraceEnricher.cs`, `MachineProcessEnricher.cs` (cached machine/process), `EnvironmentEnricher.cs` (envName / region / appVersion / appRepoName, with `BEEP_ENV` / `BEEP_REGION` env-var fallbacks), `ActivityScopeEnricher.cs` for scope name/start/tags; key constants live in internal `EnrichmentProperties.cs`)_
- [x] P06-03 Tenant + user enrichers (pulled from `IDMEEditor` / ambient principal). _(Engine · `TenantEnricher.cs` and `UserEnricher.cs` accept `Func<string>` resolvers so they don't take a hard dependency on `IBeepService`; concrete principal/repo bridges land in Phase 09)_
- [x] P06-04 Bridge to `System.Diagnostics.Activity` for OTel compatibility. _(Engine · `TraceEnricher.TryReadAmbient` prefers `Activity.Current` (W3C format) over `BeepActivityScope.Current` so envelopes inherit ASP.NET Core/gRPC/HttpClient spans; `BeepActivityScope.Begin` also inherits the ambient `Activity` trace id when one exists so nested Beep scopes stay correlated with the framework span)_
- [x] P06-05 Helper `using (BeepActivityScope.Begin("OperationName")) { ... }`. _(Engine · `Services/Logging/BeepLogScopeExtensions.cs` adds `IBeepLog.Scope(name, tags)` returning `IDisposable`; safe when log is null/disabled or name is empty)_
- [~] P06-06 Tests for AsyncLocal across `await`, scope nesting, OTel inherit. _(deferred to Phase 13 alongside P02-08 / P03-07 / P04-07 / P05-07; same rationale: no test project under `BeepDM/` yet)_

> Phase 06 build status: net9.0 build succeeds with **0 errors / 0 new warnings**. Pipeline integration was not touched — Phase 02 already runs `ApplyEnrichers` ahead of `ApplyRedactors`, matching the spec ordering (enrich → redact → sample → enqueue). Enrichers are opt-in via `BeepLoggingOptions.Enrichers` / `BeepAuditOptions.Enrichers`; with zero enrichers configured the producer-supplied `TraceId` / `CorrelationId` flow through untouched. `BeepActivityScope` is sealed `partial` across two files; the `AsyncLocal<Stack<BeepActivity>>` lives in the `.AsyncLocal.cs` partial alongside an internal `ResetCurrentForTests()` helper that the Phase 13 tests will use.

## Phase 07 — Sampling, Dedup & Rate Limiting · [`07-phase7-sampling-deduplication-and-rate-limiting.md`](./07-phase7-sampling-deduplication-and-rate-limiting.md)

- [x] P07-01 `ISampler` + `LevelSampler` / `CategorySampler` / `AlwaysSampler` / `NeverSampler`. _(Engine · `Services/Telemetry/Sampling/{LevelSampler,CategorySampler,AlwaysSampler,NeverSampler}.cs` + internal `SamplingDecision.cs` for deterministic FNV-1a hashing of correlation/trace ids; `ISampler` interface itself stays at `Services/Telemetry/ISampler.cs` (Phase 02 forward-decl is canonical))_
- [x] P07-02 `MessageDeduper` — collapse N identical messages within window into one summary. _(Engine · `Services/Telemetry/Dedup/IMessageDeduper.cs` + `WindowedDeduper.{Core,Window}.cs` partials + `MessageTemplateNormalizer.cs`; identity = `(level | category | normalized-template)`; LRU cap (default 1024 keys); on window expiry emits a synthetic `[dedup] N additional occurrences of: <template>` envelope with `dedup.count` / `dedup.template` properties)_
- [x] P07-03 `TokenBucketRateLimiter` per source/category. _(Engine · `Services/Telemetry/RateLimit/IRateLimiter.cs` + `TokenBucketRateLimiter.cs`; per-key key extractor (default `category`), `double`-precision token math, periodic `[rate-limited]` summary envelope every `SummaryInterval` (default 30s) with `rateLimit.drops` / `rateLimit.key` properties)_
- [x] P07-04 Wire into pipeline pre-queue. _(Engine · `TelemetryPipeline.{Core,Drain,Flush}.cs` updated; locked stage order Producer → Enrichment → Redaction → Sampler → Deduper → RateLimiter → Queue → Sinks; deduper/limiter receive a synthetic-emitter callback bound to `BoundedChannelQueue.TryEnqueue` so summaries cannot be deduped or throttled themselves; `FlushAsync` and `DisposeAsync` drain pre-queue stages before flushing the writer)_
- [x] P07-05 Audit always bypasses sampler/dedup/rate-limit. _(Engine · `TelemetryPipeline.SubmitLog` gates the three stages on `envelope.Kind == TelemetryKind.Log`; `SubmitAuditAsync` skips the entire stage block by construction)_
- [~] P07-06 Tests for each stage + property-based test for sampler determinism. _(deferred to Phase 13 alongside P02-08 / P03-07 / P04-07 / P05-07 / P06-06; same rationale: no test project under `BeepDM/` yet)_

> Phase 07 build status: net9.0 build succeeds with **0 errors / 0 new warnings**. `BeepLoggingOptions` exposes `Deduper` and `RateLimiter` settable properties; `BeepServiceLoggingExtensions.BuildPipeline` forwards them to the new pipeline ctor params. `TelemetryPipeline` adds `SampledOutCount`, `DedupSuppressedCount`, and `RateLimitedCount` counters that Phase 11 will surface through `PipelineMetrics`. Audit traffic bypasses all three stages by contract — `BeepAuditOptions` deliberately does **not** expose `Deduper` / `RateLimiter` so operators cannot accidentally throttle compliance events.

## Phase 08 — Audit Event Schema & Tamper Evidence · [`08-phase8-audit-event-schema-and-tamper-evidence.md`](./08-phase8-audit-event-schema-and-tamper-evidence.md)

- [x] P08-01 `Services/Audit/Models/AuditEvent.cs` (canonical schema v1). _(Models · existing `AuditEvent.cs` keeps the identity surface; new `AuditEvent.ChainFields.cs` partial adds `Category`, `Operation`, `Outcome`, `Reason`, `FieldChanges`, `ChainId`, `Sequence`, `PrevHash`, `Hash` plus `DefaultChainId` constant)_
- [x] P08-02 `AuditCategory` enum (DataAccess, Auth, Config, Schema, Distributed, Custom). _(Models · `Services/Audit/Models/AuditCategory.cs` + `AuditOutcome.cs` + `AuditFieldChange.cs` POCO carrying typed `OldValue`/`NewValue`)_
- [x] P08-03 `Services/Audit/Integrity/HashChainSigner.cs` (HMAC-SHA256 over previous-hash + payload). _(Engine · `IHashChainSigner.cs` + `HashChainSigner.{Core,Sign,Verify}.cs` partials; per-`ChainId` mutex map keeps `Sequence` strictly monotonic; canonical input via `CanonicalJsonSerializer.cs` (sorted-key JSON, ISO-8601 dates, enum names, sorted nested dictionaries — stable across .NET 8/9/10 and OS); secret sourced via pluggable `IKeyMaterialProvider` (`EnvironmentKeyMaterialProvider` reads `BEEP_AUDIT_HMAC_SECRET`, `StaticKeyMaterialProvider` for tests))_
- [x] P08-04 `Services/Audit/Integrity/IntegrityVerifier.cs` (full-chain replay). _(Engine · `IntegrityVerifier.cs` façade + `IAuditEventReader.cs` storage-agnostic source + `IntegrityIssueKind` / `IntegrityIssue` / `IntegrityCheckResult` payload types; `HashChainSigner.VerifyChain` returns the first divergence with `(file? sequence, expected, actual)`-style detail covering `SequenceGap`, `PrevHashMismatch`, `HashMismatch`, `ChainIdMismatch`, `AnchorMismatch`, `PayloadInvalid`)_
- [x] P08-05 Optional sealed-log mode (file marked read-only after rotate). _(Engine · `Services/Audit/Integrity/SealedLogPolicy.cs` sets `FileAttributes.ReadOnly` on Windows and additionally `chmod 0440` on Linux/macOS via `File.SetUnixFileMode`; never throws — exposes `OnSealed` / `OnFailed` callbacks for Phase 11; wired in DI when `BeepAuditOptions.SealRotatedFiles` is true (default))_
- [x] P08-06 Document the v1 schema + reserved fields. _(documented inline in the `AuditEvent.ChainFields` partial: `EventId`, `TimestampUtc`, `ChainId`, `Sequence`, `PrevHash`, `Hash` are reserved; producers must not write `Sequence` / `PrevHash` / `Hash`; `ChainId` defaults to `"default"`; `Category=Unspecified` is permitted for legacy producers)_

> Phase 08 build status: net8.0 + net9.0 + net10.0 build succeeds with **0 errors / 0 new warnings**. Tamper-evidence is on by default — `BeepAuditOptions.HashChain = true` causes the DI extension to compose `EnvironmentKeyMaterialProvider` + `JsonChainAnchorStore` (under `PlatformPaths.AuditDir()`) + `HashChainSigner` and inject the signer into `BeepAudit.RecordValueTaskAsync`, which signs each `AuditEvent` BEFORE it reaches the queue (a sign failure drops the event by design — a missing tamper stamp must never escape the audit boundary). The chain anchor file is written atomically (`tmp + Flush(true) + Replace/Move`) so a crash mid-write either keeps the prior anchor or commits the new one; restart resumes the chain from `LastHash`. `BeepAuditOptions` adds typed override hooks (`KeyMaterial`, `AnchorStore`, `Signer`, `AnchorStoreDirectory`, `SealRotatedFiles`). Audit `FileRollingSink`s have their `Rolled` event auto-subscribed to `SealedLogPolicy.Seal(...)` when sealed-log mode is enabled. Tests for the chain (10k events, single-byte mutation, restart continuity, purge re-seal) are deferred to Phase 13 alongside P02-08 / P03-07 / P04-07 / P05-07 / P06-06 / P07-06; same rationale: no test project under `BeepDM/` yet.

## Phase 09 — Integration With Existing Subsystems · [`09-phase9-integration-with-existing-subsystems.md`](./09-phase9-integration-with-existing-subsystems.md)

- [x] P09-01 `Services/Logging/Bridges/DMLoggerToBeepLogBridge.cs` (so legacy `lg.WriteLog(...)` flows through pipeline). _(Engine · `Services/Logging/Bridges/DMLoggerToBeepLogBridge.cs`; implements every `IDMLogger` member, raises `Onevent` for UI subscribers, honors `StartLog`/`StopLog`/`PauseLog`, supports `AddLogFilter`, swallows downstream faults so the legacy data path never breaks)_
- [x] P09-02 `Services/Logging/Bridges/MicrosoftLoggerProvider.cs` (expose `IBeepLog` as an `ILoggerProvider`). _(Engine · `MicrosoftLoggerProvider.cs` + per-category `MicrosoftLoggerAdapter.cs` + `MicrosoftLoggerScope.cs`; full level mapping (`Trace`..`Critical`+`None`), structured-state extraction, EventId promotion to `mel.eventId`/`mel.eventName`, MEL `BeginScope` lifted into `BeepActivityScope.Begin` so HTTP/EF/grpc scopes inherit Beep correlation/trace ids)_
- [x] P09-03 `Services/Audit/Bridges/FormsAuditBridge.cs` (forward `AuditManager` entries to `IBeepAudit`). _(Engine · `FormsAuditBridge.cs` maps legacy `Editor.Forms.Models.AuditEntry` → `Services.Audit.Models.AuditEvent` (`Source = "Forms.Block.{BlockName}"`, `Category = DataAccess`, `Outcome` derived from `AuditOperation`, before/after images surfaced in `Properties`); `AuditStoreSaveExtensions.SaveAndForward(IAuditStore, AuditEntry, FormsAuditBridge)` provides the dual-write entry point so call sites migrate without losing legacy query behaviour)_
- [x] P09-04 `Services/Audit/Bridges/ProxyAuditBridge.cs` (`IProxyAuditSink` → `IBeepAudit`). _(Engine · `ProxyAuditBridge.cs` implements `IProxyAuditSink.Write` with strict no-throw semantics; emits `Source = "Proxy.Operation.{OperationName}"`, `Category = Custom`, `Outcome = Success/Failure`, surfaces `selectedSource`/`totalAttempts`/`elapsedMs`/`safety`/`fanOutSucceeded`/`attempts.count` on `Properties`; preserves legacy `FileProxyAuditSink` operation when both are wired in chain)_
- [x] P09-05 `Services/Audit/Bridges/DistributedAuditBridge.cs` (`IDistributedAuditSink` → `IBeepAudit`, when distributed phase 13 lands). _(Engine · skeleton `DistributedAuditBridge.cs` exposes `ForwardAsync(component, operation, outcome, properties, ct)` so resharding planner / transaction coordinator can write through it today; will implement the canonical `IDistributedAuditSink` once that contract lands without breaking these call sites)_
- [x] P09-06 Compatibility shim: existing `lg` API still works when feature is off. _(default — `BeepLoggingOptions.Enabled = false` keeps the original `DMLogger` registration; `BeepLoggingOptions.ReplaceDMLogger` (default `true`) gates the bridge swap; with the feature off there is **zero** new allocation on the legacy code path; `RegisterMicrosoftLoggerProvider` follows the same pattern for MEL)_

> Bridge wiring lives in `Services/Audit/Bridges/AuditBridgeRegistry.cs` (idempotent `TryAddSingleton` for `FormsAuditBridge`, `ProxyAuditBridge`, `DistributedAuditBridge` — and registers `ProxyAuditBridge` as the active `IProxyAuditSink` when `BridgeProxy = true`). `BeepServiceAuditExtensions.AddBeepAudit` now invokes the registry after constructing the audit pipeline. `BeepServiceLoggingExtensions.AddBeepLogging` performs the symmetric swap for `IDMLogger` (using `ServiceCollectionDescriptorExtensions.RemoveAll<IDMLogger>()` + the bridge factory) and for `ILoggerProvider`.

> Phase 09 build status: net8.0 + net9.0 + net10.0 build succeeds with **0 errors / 0 new warnings**. All bridges absorb downstream faults so a misbehaving unified sink can never break the legacy commit / write / log path. Tests for forms / proxy / MEL forwarding (P09-07 in the phase doc) are deferred to Phase 13 alongside P02-08 / P03-07 / P04-07 / P05-07 / P06-06 / P07-06; same rationale: no test project under `BeepDM/` yet.

## Phase 10 — Query, Export, Purge & Compliance · [`10-phase10-query-export-purge-and-compliance.md`](./10-phase10-query-export-purge-and-compliance.md)

- [x] P10-01 `AuditQuery` builder (`AuditQuery.{Core,Filters}.cs`) + filters (category, user, tenant, entity, date range, source, outcome, chain id, properties, order, take). _(Models · `DataManagementModelsStandard/Services/Audit/Models/AuditQuery.{Core,Filters}.cs` partials extending the Phase 01 bag with the fluent API and an in-memory `Matches(AuditEvent)` matcher reused by every engine)_
- [x] P10-02 `IAuditQueryEngine` + storage-specific engines (`SqliteAuditQueryEngine`, `FileScanAuditQueryEngine`, `CompositeAuditQueryEngine`); `SqliteSink.QueryAudit.cs` & `SqliteSink.PurgeAudit.cs` partials make the SQLite sink double as an engine + purge store; `NdjsonAuditDeserializer.cs` reconstructs Phase 08 chain fields from disk lines. _(Engine · `Services/Audit/Query/`)_
- [x] P10-03 `Services/Audit/Export/AuditExporter.{Core,Csv,Json,Ndjson}.cs` (NDJSON wraps each event in the canonical envelope so on-disk hashes verify; JSON emits `{ events: [...] }`; CSV embeds field-changes/properties as JSON cells) + `ExportManifest.cs` + `ManifestSigner.cs` (HMAC-SHA256 over canonical manifest, key from same `IKeyMaterialProvider` as the chain) + `AuditExportResult.cs` + `ExportFormat.cs`; atomic `WriteToFiles` produces `<file>` + `<file>.manifest.json`.
- [x] P10-04 GDPR purge: `IPurgePolicy.cs` + `ConfirmTokenPurgePolicy.cs` + `GdprPurgeService.{Core,User,Entity,ResealChain}.cs`; `IAuditPurgeStore.cs` + `PurgeImpact.cs` define the storage contract; `HashChainSigner.Reseal.cs` re-numbers + re-hashes survivors and updates the in-memory state and `IChainAnchorStore` so the chain stays continuous after the deletion; every successful purge writes a synthetic `Custom`/`PurgeBy{User|Entity}` event to the dedicated `"purge"` chain (operator id, removed count, affected chain ids).
- [x] P10-05 Log query: `LogQuery.cs` + `LogRecord` + `ILogQueryEngine.cs` + `SqliteLogQueryEngine.cs` + `FileScanLogQueryEngine.cs` + `CompositeLogQueryEngine.cs`; `NdjsonLogDeserializer.cs` skips audit lines so the log channel never bleeds; `SqliteSink.QueryLog.cs` translates the filter into parameterized SQL over `kind = 0`.
- [~] P10-06 Tests for query, export+verify, purge+re-seal _(deferred to Phase 13 alongside the rest of the test set; same rationale: no test project under `BeepDM/` yet)_.

> Phase 10 build status: **net8.0 + net9.0 + net10.0 build succeeds with 0 errors / 0 new warnings on the new files**. `BeepAudit` gained a `void AttachComplianceServices(IAuditQueryEngine, GdprPurgeService, IntegrityVerifier)` late-bind hook; `BeepAudit.QueryAsync` flushes the pipeline and delegates to the configured engine; `PurgeBy{User|Entity}Async` requires `BeepAuditOptions.PurgeConfirmationToken` (or a custom `IPurgePolicy`) and refuses to run without it; `VerifyIntegrityAsync` walks every observed chain via the query engine and the Phase 08 `IntegrityVerifier`. DI extensions: `BeepServiceExtensions.Audit.cs` now also registers `IAuditQueryEngine` (composite of every SQLite + opt-in file-scan engine), `ManifestSigner` and `AuditExporter` (when `HashChain` is on); `BeepServiceExtensions.Logging.cs` registers `ILogQueryEngine` (composite of every SQLite + file engine). `NdjsonSerializer.WriteAudit` was extended additively with the Phase 08 chain fields + `field_changes` array so a file-scan query round-trips the canonical envelope cleanly. `BeepAuditOptions` exposes `PurgeConfirmationToken`, `PurgePolicy`, `EnableFileScanQuery`, `FileScanDirectory`, `FileScanPrefix`. `IHashChainSigner.Reseal(...)` is the new contract method consumed by the purge service.

## Phase 11 — Self-Observability & Health · [`11-phase11-self-observability-and-health.md`](./11-phase11-self-observability-and-health.md)

- [x] P11-01 `Services/Telemetry/Diagnostics/PipelineMetrics.{Core,Counters,Gauges,Snapshot}.cs` (queue depth, drop count by stage, batch latency, chain + self-event totals) backed by `Interlocked` operations + `MetricsSnapshot.cs` POCO. `TelemetryPipeline.Metrics` is the single read surface; counters are wired from `TelemetryPipeline.Drain` (enqueued / sampled-out / deduped / rate-limited / queue-full) and `BatchWriter` (`onSinkError` + `onFlushLatency` callbacks).
- [x] P11-02 Sink health probe: `Services/Telemetry/Diagnostics/{ISinkHealthProbe.cs, SinkHealth.cs, HealthAggregator.cs}`. `FileRollingSink.Health.cs` and `SqliteSink.Health.cs` implement `ISinkHealthProbe` and record last-success / last-error / consecutive failures inside the existing write path. Aggregator falls back to bare `ITelemetrySink.IsHealthy` for sinks that do not implement the probe.
- [x] P11-03 Backpressure / sink-error / sweeper / chain events surfaced via `Services/Telemetry/Diagnostics/SelfEventEmitter.cs` under category `BeepTelemetry.Self.*` (`SelfEventCategory.cs` defines the reserved roots). Self events bypass dedup / rate-limit / sampler stages by routing through `TelemetryPipeline.EnqueueSelfEvent` so a failure cannot be hidden by its own throttler. Per-(category, key) dedup window (default 1 minute) is enforced inside the emitter.
- [x] P11-04 Storage-budget breach + sweeper events surfaced via `Services/Telemetry/Diagnostics/PipelineDiagnosticsHooks.cs` which subscribes to `IBudgetEnforcer.Swept` (deletes / compress / breach counters + a single dedup'd self event per scope) and to `TelemetryPipeline.SinkErrored` (per-sink error self event). Chain operations increment `chain_signed_total` (`BeepAudit.Core`), `chain_verified_total` and `chain_divergence_total` (`BeepAudit.Query`).
- [x] P11-05 Optional periodic snapshot: `Services/Telemetry/Diagnostics/PeriodicMetricsSnapshotHostedService.cs` (off by default), `MetricsSnapshotRenderer.cs` (text + JSON), `MetricsSnapshotFormat.cs` enum. Atomic `tmp -> rename` writes; can also emit each snapshot as a `BeepTelemetry.Self.Snapshot` event when `EmitMetricsSnapshotAsSelfEvent = true`.
- [~] P11-06 Tests for counters, self events, and snapshot writer _(deferred to Phase 13 alongside the rest of the test set; same rationale as Phase 10)_.

> Phase 11 build status: **net8.0 + net9.0 + net10.0 build succeeds with 0 errors / 0 new warnings on the new files**. Operator surface (per pipeline): `BeepLoggingOptions` / `BeepAuditOptions` expose `EnableMetricsSnapshot`, `MetricsSnapshotInterval`, `MetricsSnapshotFile`, `MetricsSnapshotFormat`, `EmitMetricsSnapshotAsSelfEvent`. DI: `BeepServiceExtensions.Diagnostics.cs` is the single hook-attachment point — it is invoked from both `BeepServiceLoggingExtensions.AddBeepLogging` and `BeepServiceAuditExtensions.AddBeepAudit` to register a keyed `SelfEventEmitter` (one per pipeline), wire `PipelineDiagnosticsHooks`, and conditionally schedule the snapshot hosted service. `TelemetryPipeline` now exposes `Name`, `Metrics`, and `EnqueueSelfEvent`; the `SinkErrored` event lets host-level observers fan out without subscribing to every sink. `BatchWriter` accepts `onSinkError` / `onFlushLatency` callbacks (back-compat by making both optional). No dynamic allocations on the hot drain path; all counters are `Interlocked`.

## Phase 12 — Platform Targets (Blazor / MAUI / Server) · [`12-phase12-platform-targets-blazor-maui-server.md`](./12-phase12-platform-targets-blazor-maui-server.md)

- [x] P12-01 `BeepServiceExtensions.Desktop.cs` (`AddBeepLoggingForDesktop` + `AddBeepAuditForDesktop`): `LocalApplicationData/{appName}` paths via `PlatformPaths`, `FileRollingSink` (10 MB rotation), `PlatformBudgets.DesktopLogBytes` (50 MB) for logs and `DesktopAuditBytes` (200 MB) for audit, `DropOldest` for logs / `Block` for audit, retention sweeper on by default, `CompressOnRotate = true`.
- [x] P12-02 `BeepServiceExtensions.Web.cs` (`AddBeepLoggingForWeb` + `AddBeepAuditForWeb`): `{contentRoot}/logs` and `/audit` (with safe fallback to `PlatformPaths` when ContentRoot is read-only), `FileRollingSink` (20 MB rotation), `PlatformBudgets.WebLogBytes` (500 MB) / `WebAuditBytes` (≈2 GB), higher `WebQueueCapacity` (50k), retention sweeper on, gzip on rotate.
- [x] P12-03 `BeepServiceExtensions.Blazor.cs` (`AddBeepLoggingForBlazor` + `AddBeepAuditForBlazor`): host-supplied `IIndexedDbBridge` consumed by `BlazorIndexedDbSink` (.Core/.Write/.Health partials), `PlatformBudgets.BlazorLogBytes` (5 MB) / `BlazorAuditBytes` (5 MB), small `BlazorQueueCapacity` (1k), retention sweeper off (sink prunes itself), `CompressOnRotate = false`. Audit on Blazor is opt-in only.
- [x] P12-04 `BeepServiceExtensions.Maui.cs` (`AddBeepLoggingForMaui` + `AddBeepAuditForMaui`): caller-supplied `Func<string>` returning `FileSystem.AppDataDirectory`, `MauiAppDataSink.Create(...)` factory wrapping `FileRollingSink` (1 MB rotation, 15 min interval), `PlatformBudgets.MauiLogBytes` (20 MB) / `MauiAuditBytes` (40 MB), `MauiQueueCapacity` (2k), `CompressOnRotate = false` (CPU-bound).
- [x] P12-05 Audit helpers shipped alongside each platform's logging helper (single file per platform: `*.Desktop.cs`, `*.Web.cs`, `*.Blazor.cs`, `*.Maui.cs` — both features registered side-by-side per the “one class per file” rule).
- [x] P12-06 Storage-budget recommendations centralized in `Services/Telemetry/Presets/PlatformBudgets.cs` (single source of truth for desktop / web / Blazor / MAUI log + audit caps, queue capacities, and per-file rotation caps). README rollup deferred to Phase 13 (P13-06).
- [~] P12-07 Tests per platform _(deferred to Phase 13 — same rationale as P10/P11; tracked under P13-04 sample apps and P13-03 perf harness)_.

> Phase 12 build status: **net8.0 + net9.0 + net10.0 build succeeds with 0 errors / 0 new lint warnings on the new files**. Operator surface (per host): six new public extension methods (`AddBeepLogging|AuditFor{Desktop,Web,Blazor,Maui}`) — each registers a single `FileRollingSink` (or `BlazorIndexedDbSink`) at the platform-correct path, locks the `PlatformBudgets` defaults, and then defers to the canonical `AddBeepLogging` / `AddBeepAudit` so every Phase 02-11 wiring (sinks, enrichers, redactors, retention sweeper, hash chain, query/export/purge, self-observability) keeps a single entry point. New types: `Services/Telemetry/Presets/PlatformBudgets.cs`, `Services/Telemetry/Sinks/Platform/IIndexedDbBridge.cs`, `Services/Telemetry/Sinks/Platform/BlazorIndexedDbSink.{Core,Write,Health}.cs`, `Services/Telemetry/Sinks/Platform/MauiAppDataSink.cs`. The library still takes **zero** hard dependencies on `Microsoft.JSInterop` / `Microsoft.Maui.Storage` / `Microsoft.AspNetCore.Hosting` — Blazor goes through the abstract `IIndexedDbBridge`, MAUI through a `Func<string>` directory provider, and Web accepts an explicit `contentRootPath` string with safe fallback. `MauiAppDataSink` is a static factory rather than a subclass because `FileRollingSink` is sealed.

## Phase 13 — DevEx, Testing, Rollout & Docs · [`13-phase13-devex-testing-rollout-and-docs.md`](./13-phase13-devex-testing-rollout-and-docs.md)

- [ ] P13-01 `RecordingSink` (in-memory, queryable) for tests.
- [ ] P13-02 Fault injection: `FlakySink`, `SlowSink`, `FullDiskSink`.
- [ ] P13-03 Performance harness (10k events/sec target on desktop).
- [ ] P13-04 Sample apps under `Services/Examples/` (Desktop/Web/Blazor) showing opt-in.
- [ ] P13-05 Runbook: enable/disable, rotate, purge, recover from corrupt audit chain.
- [ ] P13-06 Update `Services/README.md` and `Services/IMPLEMENTATION_SUMMARY.md`.

---

## Cross-cutting acceptance gates

- [ ] G-01 No new Windows-only API usage (CI grep gate).
- [ ] G-02 All sinks implement `IDisposable` and flush on dispose.
- [ ] G-03 With features OFF, allocations and CPU are within ±2% of baseline.
- [ ] G-04 With features ON at default budgets, an 8-hour soak does not exceed configured `StorageBudgetBytes`.
- [ ] G-05 Tamper-evident audit chain verifies after rotation, purge, and process restart.
- [ ] G-06 `BeepService.Stop()` flushes pipeline within configured `ShutdownTimeout` (default 5s).
