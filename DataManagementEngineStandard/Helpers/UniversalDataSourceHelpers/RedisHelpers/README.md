# Redis Helper

## Purpose
This folder contains Redis-specific helper logic used to bridge key/value and in-memory structures with BeepDM helper contracts.

## Key Files
- `RedisHelper.cs`: Redis helper implementation.

## Usage Notes
- Keep key naming and namespace patterns consistent.
- Reflect limited relational support via capability checks.
- Align type conversion with Redis storage representations.
